-- query that Fetches all the fields in a table 
-- (limit the data to 20 rows,our dataset is a bit large)

SELECT * FROM city LIMIT 20;
SELECT * FROM country LIMIT 20;
SELECT * FROM countrylanguage LIMIT 20;
